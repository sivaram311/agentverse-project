# Hire note — parallel Layer B workers (template)

**Date:** YYYY-MM-DD  
**Session:**  
**App pack:** `<appId>`  
**Lead:** Rajesh / Integrate Lead  
**Why:** complete quicker via parallel subagents (not all on stage)

## Workers

| Lane | Persona id | Name | Ownership (paths) | Tool | Exit |
|------|------------|------|-------------------|------|------|
| A | | | | Task / agent CLI | |
| B | | | | | |
| C | | | | | |

## Rules

- Disjoint file ownership — no two lanes edit same path.
- Playwright: **one** claim for all browser projects (`PLAYWRIGHT-SLOT`).
- Single-shot per lane ask.
- On pack **Switch** with conflicts: Lead retires lanes and re-hires.
- ACTIVITY-LOG one row per lane + integrate row.
- Stage avatars optional — do not spawn WebGL bodies for every worker.

## Integrate

- [ ] Merge lanes  
- [ ] Docs #12  
- [ ] E2E / promote only if in scope  
