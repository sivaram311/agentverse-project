import { AgentVerseApp } from "@/components/AgentVerseApp";

/** Deep-link entry — same shell as `/`; query contract in docs/DEEP-LINK-CONTRACT.md */
export default function DeskPage() {
  return <AgentVerseApp />;
}
